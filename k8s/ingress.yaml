apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wishlist
spec:
  ingressClassName: traefik
  rules:
    - host: localhost
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: wishlist
                port:
                  number: 80
